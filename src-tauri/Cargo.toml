[package]
name = "ai-content-modeling-desktop"
version = "0.1.0"
description = "AI Content Modeling desktop client"
authors = ["Phong"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
