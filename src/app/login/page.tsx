"use client";
import { useState } from "react";

export default function LoginPage() { const [mode, setMode] = useState<"login" | "register">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [error, setError] = useState(""); async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch(`/api/auth/${mode}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email, password, ...(mode === "register" ? { name } : {}) }) }); if (!response.ok) { const body = await response.json(); setError(body.error?.message ?? "Không thể xác thực."); return; } window.location.href = "/"; } return <main className="flex min-h-screen items-center justify-center bg-[#f4f7fb] p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-[#d8e3f1] bg-white p-8 shadow-lg"><p className="text-xs font-extrabold tracking-[0.18em] text-teal-600">MODELING AI</p><h1 className="mt-2 text-3xl font-extrabold text-[#0b3262]">{mode === "login" ? "Đăng nhập" : "Tạo tài khoản"}</h1><p className="mt-2 text-sm text-[#6883aa]">Truy cập workspace AI Content Modeling của bạn.</p>{mode === "register" && <label className="mt-6 block text-sm font-semibold text-[#0b3262]">Tên<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-lg border border-[#cbd9ea] px-3 py-3" /></label>}<label className="mt-5 block text-sm font-semibold text-[#0b3262]">Email<input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} className="mt-2 w-full rounded-lg border border-[#cbd9ea] px-3 py-3" /></label><label className="mt-5 block text-sm font-semibold text-[#0b3262]">Mật khẩu<input type="password" required minLength={10} value={password} onChange={(event) => setPassword(event.target.value)} className="mt-2 w-full rounded-lg border border-[#cbd9ea] px-3 py-3" /></label>{error && <p className="mt-4 text-sm text-rose-600">{error}</p>}<button className="mt-6 w-full rounded-lg bg-[#07865f] py-3 font-bold text-white">{mode === "login" ? "Đăng nhập" : "Tạo tài khoản"}</button><button type="button" onClick={() => { setMode(mode === "login" ? "register" : "login"); setError(""); }} className="mt-4 w-full text-sm font-bold text-[#0b5799]">{mode === "login" ? "Chưa có tài khoản? Đăng ký" : "Đã có tài khoản? Đăng nhập"}</button></form></main>; }
