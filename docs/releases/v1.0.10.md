# Modeling AI v1.0.10 — audit chu trình Stage 1–5

Bản này đóng gói các sửa lỗi được xác nhận trong chu trình thành công gần nhất trên run `cmuevtc5q0002k5tcbox0a2t4`, project `cmuewa0v10001k5j4z3l7bfuw`, nguồn `@kennytoons2`. Mục tiêu của bản vá là giữ đúng bằng chứng nguồn, không gửi nhầm prompt/reference, giữ checkpoint có thể resume, và chỉ bàn giao video cuối đã được xác minh.

## Kết quả chu trình

| Stage | Kết quả đã xác nhận | Lỗi/rủi ro đã xử lý |
| --- | --- | --- |
| 1 — Source analysis / Modeling Idea | PASS — tạo Modeling Idea `Bùn Tự Giật Mình Vì Tiếng Ngáy` | URL Reel không đủ làm bằng chứng nội dung; luồng phải đính kèm video nguồn thật qua CDP trước khi phân tích và đính kèm ảnh nhân vật chính trước prompt modeling. |
| 2 — Content Project | PASS — tạo đúng 1 phân cảnh | Gemini có thể trả storyboard lệch số lượng/thứ tự nguồn; schema repair nay bắt buộc cùng số cảnh, sceneNumber liên tục và sourceSceneId đúng vị trí, không merge/split/add/remove cảnh. Resume Stage 2 bị kill cũng được hạ an toàn về checkpoint paused. |
| 3 — Images | PASS — lưu 2 ảnh vào app | Renderer có thể không đọc được URL ảnh cục bộ trong runtime đóng gói; Electron IPC nay kiểm tra file thật và SHA-256. Continuity gate vẫn chặn thiếu ảnh hoặc ảnh cảnh trùng byte. |
| 4 — Gemini video via CDP | PASS — tạo và lưu 1 video phân cảnh | Bấm DOM có thể không gửi prompt dù giao diện trông như đã bấm. Sau 1.2 giây, app kiểm tra composer; nếu prompt còn lại thì thực hiện một pointer recovery có giới hạn, rồi dừng có mã `GEMINI_SUBMISSION_NOT_CONFIRMED` nếu vẫn chưa gửi. Luồng vẫn giữ Pro, 9:16, reference preview và một conversation dùng chung. |
| 5 — Final assembly / cleanup | PASS — nhận và lưu final video | Chỉ xóa media trung gian sau khi FFmpeg xác nhận MP4 hợp lệ; giữ lại `final.mp4` duy nhất trong project. |

## Bằng chứng đầu ra

- Trạng thái run: `SUCCEEDED`, `lastCompletedStage=5`, không còn execution lease.
- Final file: `C:\Users\Admin\AppData\Roaming\ai-content-modeling\generated-videos\cmuewa0v10001k5j4z3l7bfuw\final.mp4`.
- FFmpeg: H.264, 720×1280, DAR 9:16, thời lượng 10.01 giây.
- Sau cleanup: thư mục `generated-images` của project không còn; thư mục `generated-videos` chỉ còn `final.mp4` (4,022,207 bytes).

## Thay đổi được đóng gói

- CDP send-boundary confirmation và pointer recovery giới hạn một lần.
- Content Project scene-count/order/source mapping gate.
- Local generated-image validation fallback và hash continuity gate.
- Edge executable discovery fallback cho Windows.
- Prompt sanitization cho URL nguồn và tên style bên thứ ba có thể kích hoạt provider refusal; không thay đổi scene evidence hoặc action order.
- Stale Stage 2 checkpoint recovery.
- Regression tests cho các đường biên trên và timeout ổn định cho test FFmpeg dưới tải song song.

## Kiểm tra gói phát hành

- `npm test`: PASS — 82 test files, 914 tests.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS.
- `npm run build`: PASS; các cảnh báo `<img>`/unused variables hiện hữu không chặn build.
- Electron NSIS: PASS — `Modeling.AI.Setup.1.0.10.exe`.
- Clean-install smoke trên `win-unpacked`: PASS — DB khởi tạo sạch, restart giữ nguyên schema/config, cleanup userData tạm thành công.
- Gói có đủ `electron/main.cjs`, các helper lifecycle/cleanup, `package.json` trong `app.asar`, và FFmpeg hợp lệ trong `app.asar.unpacked`.
- SHA-256 installer: `FE343678AC71E3EAD303433857A1B601C2D22ACAF4A97C66B53879DD028499DF`.

## Giới hạn còn giữ nguyên

- Anti-abuse/unusual-activity của Google Flow/Gemini là quyết định bên ngoài; app không bypass và không retry vô hạn.
- Kiểm tra codec, kích thước, hash và checkpoint không chứng minh chất lượng diễn xuất AI; người dùng vẫn cần duyệt nội dung video cuối.
