# Modeling AI v1.0.11 — Stage 5 duration-safe assembly

Bản vá này xử lý lỗi video cuối dài 43,87 giây dù storyboard chỉ có 5 cảnh với tổng thời lượng mục tiêu 14,2 giây. Nguyên nhân là Stage 5 nối nguyên thời lượng MP4 do Gemini/Flow trả về thay vì cắt theo thời lượng storyboard.

## Thay đổi

- Mỗi cảnh được cắt theo `targetDuration` đã được Stage 2 phê duyệt.
- `trimStart` và `trimEnd` vẫn được tôn trọng khi người dùng edit thủ công.
- Cảnh ngắn hơn thời lượng yêu cầu bị từ chối trước khi ghép.
- Video cuối bị từ chối nếu thời lượng thực tế lệch quá 0,25 giây so với thời lượng dự kiến sau chuyển cảnh.
- Áp dụng đồng nhất cho Electron Stage 5 và server-side/Codex assembly.

## Kiểm thử

- `npm test`: PASS — 83 test files, 920 tests.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS — chỉ còn các cảnh báo đã tồn tại trước đó.

## Lưu ý video hiện tại

Run `cmuf0civ90046k518853rkvjb` đã bị cleanup các MP4 cảnh trung gian, chỉ còn `final.mp4`. Vì vậy không thể cắt lại chính xác video cũ bằng bản vá này; cần chạy lại Stage 4 để tạo lại 5 MP4 cảnh, sau đó Stage 5 sẽ ghép thành bản đúng thời lượng.
